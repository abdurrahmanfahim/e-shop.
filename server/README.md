# E-Shop Server

REST API for the E-Shop e-commerce frontend.

## Setup

```bash
cd server
cp .env.example .env
npm install
npm run dev
```

## Seed Database

```bash
node scripts/seedProducts.js
```

## API Docs

See `CONTEXT.md` for full endpoint reference.
